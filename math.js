const math = require("./index.js");
const fs = require("fs");
const crypto = require("crypto");
const express = require("express");
// console.log(express);
// console.log(crypto);
const app = express();
console.log(app);
app.listen();

console.log(math(9, 8));
